{
  "name": "back-office-jobs",
  "description": "BackOffice Jobs serverless project",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "db-migration:create": "migrate-mongo create",
    "db-migration:up": "migrate-mongo up",
    "db-migration:down": "migrate-mongo down"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "dotenv": "10.0.0",
    "husky": "7.0.4",
    "jest": "^28.1.3",
    "migrate-mongo": "8.2.3",
    "ts-jest": "^28.0.8"
  },
  "engines": {
    "npm": ">=8.5.0",
    "node": ">=14.17.2"
  },
  "repository": {
    "url": "https://github.com/double-ro/back-office-jobs.git"
  }
}
